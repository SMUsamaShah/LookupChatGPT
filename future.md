# Future Ideas

## Features
- [ ] Markdown rendering in result panel
- [ ] Keyboard shortcut to trigger last-used prompt on selected text
- [ ] Per-prompt keyboard shortcuts (Alt+1, Alt+2, …)
- [ ] Ollama provider (localhost, no API key, fully offline)
- [ ] Auto-copy output mode — silently copy result to clipboard, no popup
- [ ] Right-click on images — pass image URL/alt text to a prompt
- [ ] Result panel position options (top-left / top-right / near cursor)
- [ ] Last N results history in toolbar popup

## Data / Settings
- [ ] Switch to chrome.storage.sync so settings roam across devices (mind the 100 KB quota)
- [ ] Export / import prompts and settings as JSON
- [ ] Built-in prompt library — gallery of ready-made templates users can import
- [ ] Usage counter per provider (request count, rough token cost)
